# 画像ランダム分割・散開配置ツール 要件定義書

## 1. プロジェクト概要

ユーザーが読み込んだ画像を、指定した数の不規則な形状の画像片（ピース）に分割し、それぞれのピースをランダムに移動・回転させて、バラバラに配置した1枚の画像を生成するPythonアプリケーションを開発する。

初期バージョンはローカル環境で動作するPythonプログラムとして実装し、Git/GitHubでバージョン管理を行う。

将来的にはGUI化やWebアプリケーション化も検討するが、まずは画像処理部分の完成を優先する。

---

## 2. 目的

- 任意の画像を不規則な形状に分割できるようにする。
- ユーザーが分割数やランダム性を調整できるようにする。
- 分割された画像片を元画像の位置から散らし、画像が砕けたような表現を生成する。
- 生成結果を透明背景のPNG画像として保存できるようにする。
- 機能ごとに責務を分離し、後からGUI/Web版へ発展させやすい構造にする。

---

## 3. 対象環境

### 開発言語

- Python 3

### 主なライブラリ候補

- NumPy：座標計算、配列処理
- OpenCV：画像読み込み、画像処理
- SciPy：Voronoi分割などの幾何処理
- Pillow：RGBA画像、回転、合成、PNG出力

必要なライブラリは `requirements.txt` で管理する。

---

## 4. 基本処理フロー

```text
画像を読み込む
    ↓
入力値を受け取る
    ↓
分割用の基準点を生成する
    ↓
画像を不規則な領域に分割する
    ↓
各領域を透明背景の画像片として切り出す
    ↓
各画像片の移動量・回転角を決定する
    ↓
大きなキャンバス上へ画像片を配置する
    ↓
完成画像を生成する
    ↓
PNGとして保存する
```

---

## 5. 機能要件

### 5.1 画像読み込み

ユーザーが指定した画像ファイルを読み込む。

#### 対応形式

- PNG
- JPG
- JPEG

読み込んだ画像は、後続処理で透明領域を扱える形式へ変換する。

---

### 5.2 分割数指定

ユーザーが画像の分割数 `N` を指定できるようにする。

初期仕様では以下を想定する。

- 最小値：2
- 最大値：100
- 整数のみ

最終的に、原則として指定した `N` 個の画像片を生成する。

---

### 5.3 不規則な画像分割

画像を長方形や格子状ではなく、不規則な形状に分割する。

初期実装では **Voronoi分割** を利用する。

画像上に `N` 個の基準点を生成し、各画素を最も近い基準点に対応する領域へ分類する。

各領域が1つの画像片となる。

---

### 5.4 形状のばらつき

ユーザーが画像片の形状・大きさのばらつきを調整できるようにする。

#### 入力範囲

- 0〜100

この値は、分割基準点の規則的な初期配置からのランダム変位量として扱う。

- 0に近い：比較的均一な大きさのピース
- 100に近い：大きさや形状の差が大きいピース

完全なランダム配置だけに依存せず、値によってランダム性を段階的に制御できる実装とする。

---

### 5.5 画像片の生成

分割された各領域を独立した画像片として生成する。

各ピースについて、領域外の画素は透明にする。

画像片は長方形として見えるのではなく、分割された領域そのものの輪郭が維持されるようにする。

各画像片について、少なくとも以下の情報を管理できる構造とする。

- ピースの画像データ
- マスク
- 元画像上の位置
- 幅・高さ
- 分割領域を識別するID

---

### 5.6 散らばり量

分割した画像片を元画像の位置から移動させる。

ユーザーが散らばり量を指定できるようにする。

#### 入力範囲

- 0〜100

- 0：原則として元の位置に配置する
- 値が大きくなるほど、元の位置から大きく移動する

X方向・Y方向それぞれについてランダムな移動量を計算する。

単位や最大移動距離の具体的な計算方法は、画像サイズに応じて自然な結果になるよう設計する。

---

### 5.7 回転

各画像片をランダムに回転できるようにする。

ユーザーは最大回転角を指定する。

#### 入力範囲

- 0〜180度

例えば最大回転角が30度の場合、各画像片の回転角を原則として以下の範囲からランダムに決定する。

```text
-30° ～ +30°
```

最大回転角が0の場合は回転させない。

---

### 5.8 出力キャンバス

分割された画像片を配置するため、元画像より大きなキャンバスを生成する。

背景は透明とする。

散らばり量が大きい場合でも、可能な限り画像片全体がキャンバス内に収まるようにする。

キャンバスサイズは固定値ではなく、元画像サイズや散らばり量を考慮して決定することが望ましい。

---

### 5.9 ランダム配置

各画像片について、以下を個別に決定する。

- X方向の移動量
- Y方向の移動量
- 回転角

各ピースを出力キャンバス上へ合成し、1枚の完成画像を生成する。

ピース同士の重なりは許容する。

---

### 5.10 出力

生成結果をPNG形式で保存する。

#### 出力仕様

- PNG
- RGBA
- 背景透明

初期バージョンでは `output/` ディレクトリへ保存する。

---

## 6. ユーザー入力項目

| 項目 | 内容 | 初期範囲 |
| --- | --- | --- |
| 入力画像 | 分割対象画像 | PNG/JPG/JPEG |
| 分割数 | 画像片の数 | 2〜100 |
| 形状のばらつき | ピース形状・サイズの不均一さ | 0〜100 |
| 散らばり量 | 元位置からの移動量 | 0〜100 |
| 最大回転角 | 各ピースの最大回転量 | 0〜180° |

初期バージョンではコマンドラインから入力できればよい。

---

## 7. ランダム性

同じ入力画像・設定値でも、実行するたびに異なる結果を生成できるようにする。

将来的には `seed` を指定できる機能を追加し、以下が同じ場合には同じ結果を再現できるようにする。

```text
入力画像 + パラメータ + seed
```

初期MVPではseed指定機能は必須としない。

---

## 8. エラー処理

以下の場合は処理を中断し、原因が分かるメッセージを表示する。

- 画像ファイルが存在しない
- 画像を正常に読み込めない
- 対応していない画像形式
- 分割数が範囲外
- 数値項目に数値以外が入力された
- 形状のばらつきが範囲外
- 散らばり量が範囲外
- 最大回転角が範囲外
- 出力画像を保存できない
- その他の画像処理エラー

可能な限り、単に例外内容を表示するのではなく、ユーザーが修正方法を判断できるメッセージにする。

---

## 9. プログラム構成案

```text
random-image-splitter/
│
├─ main.py
│
├─ src/
│   ├─ __init__.py
│   ├─ splitter.py
│   ├─ piece.py
│   └─ compositor.py
│
├─ sample/
│   └─ sample.jpg
│
├─ output/
│
├─ requirements.txt
├─ .gitignore
└─ README.md
```

### main.py

- ユーザー入力
- 入力値チェック
- 各処理の呼び出し
- 出力処理

### splitter.py

- 分割基準点の生成
- 形状のばらつきの適用
- Voronoi分割
- 各領域のマスク生成

### piece.py

- 各画像片のデータ管理
- マスクを利用した画像片の切り出し
- 透明背景への変換

### compositor.py

- 散らばり量の計算
- 回転角の計算
- 出力キャンバス生成
- 各ピースの配置・合成
- 完成画像生成

---

## 10. 非機能要件

### 保守性

画像分割・画像片生成・配置処理を可能な限り分離し、1つのファイルや関数に処理を集中させない。

### 可読性

- 関数名・変数名から役割が分かるようにする。
- 複雑な画像処理にはコメントを記述する。
- マジックナンバーを可能な限り避ける。

### 拡張性

将来的に以下を追加しやすい構成とする。

- GUI
- Webアプリケーション
- リアルタイムプレビュー
- ランダムシード指定
- 分割アルゴリズムの追加
- 各画像片の個別PNG出力
- アニメーション生成

---

## 11. MVPの完成条件

初期バージョンでは以下をすべて満たした時点をMVP完成とする。

1. PNG/JPG/JPEG画像を読み込める。
2. ユーザーが分割数を指定できる。
3. 指定数を基準として不規則な領域へ画像を分割できる。
4. 形状のばらつきを0〜100で変更できる。
5. 各領域を透明背景の画像片として扱える。
6. 散らばり量を0〜100で変更できる。
7. 最大回転角を指定できる。
8. 各画像片をランダムに移動・回転できる。
9. バラバラに配置された1枚の画像を生成できる。
10. 生成結果を透明背景PNGとして保存できる。

---

## 12. 開発方針

機能を一度にすべて実装せず、以下の順番で段階的に開発する。

```text
Step 1  プロジェクト構成作成
Step 2  画像読み込み
Step 3  分割数などの入力・バリデーション
Step 4  分割基準点生成
Step 5  Voronoi分割
Step 6  画像片のマスク・切り出し
Step 7  散らばり処理
Step 8  回転処理
Step 9  キャンバスへの合成
Step 10 PNG出力
Step 11 パラメータ調整・例外処理
Step 12 README整備
```

各Stepが正常に動作することを確認してから次へ進む。

Gitでは、機能単位でコミットを作成する。

コミット例：

```text
Initial project setup
Add image loading
Add input validation
Add Voronoi splitting
Add piece extraction
Add random scattering
Add piece rotation
Add image composition
Add PNG export
```

---

## 13. Claudeに実装を依頼する際の方針

この要件定義書を基準として実装すること。

一度に全機能を実装するのではなく、原則として「12. 開発方針」のStep単位で進める。

各Stepでは、まず以下を提示すること。

1. 今回実装する機能
2. 処理の考え方
3. 変更・追加するファイル
4. 実装後に期待される動作

その後、必要なコードを提示すること。

既存コードがある場合は、既存の構造を確認したうえで変更し、不必要な全面書き換えは避けること。

また、後からGUIやWeb版へ移行できるよう、画像処理ロジックとユーザーインターフェースを可能な限り分離すること。
